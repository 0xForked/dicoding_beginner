<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/two_hundred_fifty_dp"
    android:layout_height="@dimen/two_hundred_fifty_dp"
    android:layout_margin="@dimen/three_dp">
    <RelativeLayout
        style="@style/LayoutClick"
        android:id="@+id/lay_horizontal_click">
        <ImageView
            style="@style/ImageHorizontal"
            android:id="@+id/img_horizontal"
            android:contentDescription="@null"
            android:scaleType="centerCrop"/>
        <LinearLayout
            style="@style/HorizontalCard"
            android:background="@color/white"
            android:layout_alignParentBottom="true">
            <TextView
                style="@style/Text.Main.Title"
                android:id="@+id/txt_horizontal_title"
                android:hint="@string/item_title" />
        </LinearLayout>
    </RelativeLayout>
</android.support.v7.widget.CardView>